{
  "scripts": {
    "build": "esbuild --bundle --minify --platform=node --target=node12 --outdir=build index.ts"
  },
  "devDependencies": {
    "@aws-sdk/client-sns": "^3.145.0",
    "@types/node": "^18.7.0",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.15.0",
    "typescript": "^4.7.4"
  }
}
